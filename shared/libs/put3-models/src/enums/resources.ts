export enum EnumResourceType {
    IMAGE = "image",
    VIDEO = "video",
    AUDIO = "audio",
};

export enum EnumResourceDirectory {
    IMAGES = "images",
    VIDEOS = "videos",
    AUDIOS = "audios",
};